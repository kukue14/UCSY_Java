public class No_2 {
    public static void main(String[] args) {
        int m, n, i = 3, j = 4, k = 5;
        float v, w, x = 34.5f, y = 12.25f;

        // a
        // w =Math.pow(3, Math.pow(i,j));

        // b
        v = x / i;
        System.out.println(v);

        // c
        // w = Math.ceil(y)%k;

        // d
        // n = (int)x / y * i / 2;

        // e
        // w = Math.sqrt(i * i - 4 * j * k);

        // f
        // m = n + i * j;

        // g
        // n = k / (j * i) * x + y;

        // h
        i = i + 1;
        System.out.println(i);

        // i
        w = (float) (x + i);
        System.out.println(w);

        // j
        x = x / i / y / j;
        System.out.println(x);
    }
}
